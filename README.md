<!-- 타이틀 이미지: aitoon-page-director-title.png (2:1) 를 같은 폴더에 두면 아래가 표시됩니다 -->
![aitoon-page-director](aitoon-page-director-title.png)

# aitoon-page-director

> 스토리와 미드저니 sref 스타일 이미지를 던지면, **연출된 페이지 만화를 GPT(나노바나나)로 뽑는 "복붙용 프롬프트"** 를 만들어 주는 Claude 스킬.

"스토리를 던지면 컷만화 한 장 뚝딱"이 만드는 건 대개 **글+그림 덩어리**입니다. 이 스킬은 그걸 넘어, 컷의 크기·앵글·칸 모양 같은 **연출**과 캐릭터·무기·방향의 **일관성**, 그리고 미드저니 sref **화풍**을 1차 프롬프트에 전부 박아서 — 운 좋은 한 장이 아니라 **"이렇게 하면 매번 되는"** 페이지 만화를 만듭니다.

---

## ✨ v1.3에서 새로워진 점

- **시작에서 원고 종류를 고릅니다.** 스킬을 켜면 선택 카드 위젯이 떠서 **클린 원고**(말풍선 없이 그림만)와 **대사 포함**(말풍선까지) 중 하나를 위젯 안에서 고르고 진행합니다.
- **대사 포함(말풍선) 모드 추가.** 감정에 맞는 말풍선 종류를 프롬프트에 박아 **GPT가 대사 말풍선을 직접 그려줍니다.** 검증된 말풍선 사전(`balloon-dictionary.md`)을 씁니다. 기존 **클린 원고 경로는 100% 그대로 보존**됩니다 — 말풍선은 추가 옵션일 뿐입니다.
- **일관성 가드 강화.** 캐릭터 **머리색·의상색 고정**, 다중 인물 **첨부 서수 1:1 매핑**(인물이 늘면 서수도 가변)을 연출 명세에 박아 "머리색 들쭉날쭉 · 누가 누군지 매핑 어긋남" 같은 단골 함정을 줄였습니다.

---

## 핵심 아이디어

좋은 페이지 만화는 운이 아니라 **재현 방법**입니다. 그래서 이 스킬은 연출 명세의 모든 통제를 유저가 보지 않는 1차 프롬프트 안에 녹여 한 방 성공률을 끌어올립니다.

이 스킬을 쓰는 사람은 **만화는 만들고 싶지만 과정은 귀찮은 사람**입니다. 그래서 씬 분할·컷 설계·페이지 배분·스타일 토큰 추출 같은 판단은 전부 내부에서 처리하고, 유저 눈에는 **복붙할 프롬프트만** 떨어집니다. (레이아웃 시안 같은 중간 산출물은 보여주지 않습니다.)

---

## 어떻게 동작하나

```
원고 종류 선택  →  스토리 던지기  →  스타일 이미지(sref) 첨부  →  프롬프트 복붙해서 생성  →  (선택) 결과 올려서 수정
```

유저가 손대는 접점은 시작 분기 + 이 4개뿐입니다. 나머지는 스킬이 알아서 합니다.

| 단계 | 하는 일 |
|------|---------|
| 0. 원고 종류 | 시작 카드에서 **클린 원고 / 대사 포함(말풍선)** 중 선택 (시작에서 한 번) |
| 1. 스토리 | 받아서 (내부) 씬·컷 구성 + 4~6페이지 배분 + 페이지별 감정 비트 |
| 2. 스타일 | 미드저니 sref 그리드/코드 → 스타일 토큰 7분해 + 캐릭터 경로 판정 |
| 3. 캐릭터 ref | **sref 코드 있으면 미드저니로(정석) / 없으면 GPT로(폴백)** 캐릭터 생성 프롬프트 |
| 4. 페이지 프롬프트 | 연출 3대 레버 + 일관성 + 스타일 앵커 + (클린 원고 또는 💬 말풍선)을 박은 페이지별 복붙 프롬프트 |
| 5. 사용 안내 | 첨부 순서·비율·GPT 사용법 — **여기까지가 공식 끝** |
| 6. (선택) 수정 | 결과 이미지를 올리면 그 페이지 프롬프트만 진단·수정 (자동 반복 루프는 안 돔) |

---

## 클린 원고 또는 말풍선 (시작에서 선택)

스킬을 켜면 **원고 종류**를 먼저 고릅니다.

- **클린 원고** — 말풍선·글자 없이 그림만 뽑습니다. 대사는 포토샵 등 편집 프로그램에서 직접 얹는 기존 방식입니다.
- **대사 포함(말풍선)** — 감정에 맞는 말풍선 종류를 프롬프트에 박아 **GPT가 말풍선까지 그려줍니다.** 바로 읽히는 원고가 나옵니다.

어느 쪽이든 화풍·연출·일관성 통제는 똑같이 들어갑니다. 말풍선이 아쉬우면 "말풍선 제거" 한 번으로 클린 원고로 되돌려 수동 편집할 수도 있습니다(역획득).

---

## 캐릭터는 미드저니, 페이지는 GPT (하이브리드)

```
sref 코드 보유  → 캐릭터 = 미드저니(--sref 코드, 정석)  ┐
                                                ├→ 페이지 = GPT(캐릭터 이미지 + 그리드 첨부)
그리드 이미지만 → 캐릭터 = GPT(그리드 첨부, 폴백)        ┘
```

캐릭터는 **미드저니로 원본을 잡아** 그걸 GPT 페이지에 ref로 넣는 게 정석입니다. 그래야 GPT 특유의 평평한 화풍에서 벗어나 sref 원본 화풍이 또렷이 묻습니다. 페이지(여러 컷 연출)는 미드저니가 약하므로 **GPT로 생성**합니다 — 캐릭터만 분기합니다.

---

## 설치

1. `aitoon-page-director.skill` 파일을 받습니다. (또는 이 폴더를 그대로 스킬로 설치)
2. Claude 데스크톱 **설정 > Capabilities** 에서 스킬을 설치합니다.
3. "페이지 만화 만들어줘" / "이 스토리로 컷만화 프롬프트 뽑아줘" 같은 말로 실행됩니다.

> 페이지형 컷만화(트랙 A) 전용입니다. 인스타툰 4컷·세로 스크롤 웹툰은 다른 스킬을 쓰세요.

---

## 폴더 구조

```
aitoon-page-director/
├── SKILL.md                       흐름 오케스트레이션
├── intro.html                     시작 카드(클린 원고 / 대사 포함 선택 위젯)
└── reference/
    ├── direction-rules.md         ★연출 명세 본체(컷 수·크기·앵글·칸 모양·일관성·전체 흐름·체크리스트)
    ├── style-tokens.md            sref 7분해 + 영어 앵커 + 내용물 가드
    ├── prompt-templates.md        캐릭터 ref(MJ/GPT) + 페이지 골격 + mj 규칙 내장
    ├── balloon-dictionary.md      대사 포함 분기용 말풍선 사전(복붙 스니펫)
    ├── image-qc-fix.md            (선택) 업로드 진단 → 그 페이지만 수정
    ├── gotchas.md                 쌍검·확대 QC·180도·색·서수·프레이밍 함정 등 실패 패턴
    └── examples/                  무협 ep01(v1→v2→v3) 실측 샘플
```

---

## 하는 것 / 안 하는 것

**한다** — 씬·컷 내부 구성 · 스타일 토큰 추출 · 캐릭터 ref 프롬프트 · 페이지별 복붙 프롬프트(클린 원고 또는 선택적 말풍선) · 사용 안내 · (선택) 이미지 보고 프롬프트 수정.

**안 한다** — 이미지 직접 생성(GPT·미드저니 생성은 유저 몫) · 자동 v1→v2→v3 반복 루프 · 말풍선 픽셀 합성·편집(말풍선은 GPT가 그리도록 프롬프트에 박을 뿐) · 트랙 B 세로 스크롤 웹툰.

---

## 출처 / 크레딧

제작: Blue (조남경) · Midjourney Korea
